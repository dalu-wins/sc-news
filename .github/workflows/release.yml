name: Build and Release Compose App

on:
  push:
    tags:
      - "v*" # z. B. v1.0.0, v2.3.4 etc.

jobs:
  build:
    name: Run Tests and Build Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Run Unit Tests
        run: ./gradlew test

      - name: Decode keystore
        run: |
          echo "$APP_KEYSTORE_B64" | base64 --decode > sc_news_keystore.jks
      
      - name: Build release APK
        run: ./gradlew assembleRelease \
              -Pandroid.injected.signing.store.file=sc_news_keystore.jks \
              -Pandroid.injected.signing.store.password=$APP_KEYSTORE_PASSWORD \
              -Pandroid.injected.signing.key.alias=$APP_KEY_ALIAS \
              -Pandroid.injected.signing.key.password=$APP_KEY_PASSWORD


      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: app/build/outputs/apk/release/*.apk

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download built artifact
        uses: actions/download-artifact@v5
        with:
          name: app-release
          path: app-release

      - name: List files to debug
        run: |
          echo "Files downloaded to app-release/:"
          ls -R app-release

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            app-release/**/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
